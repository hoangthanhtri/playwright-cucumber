export enum ParamTypes {
    'query' = 'query',
    'path' = 'path',
}

export enum GenerateTypes {
    'types' = 'types',
    'endpoints' = 'endpoints',
    'enums' = 'enums',
}

export enum ReportStatuses {
    'PASSED' = 'PASSED',
    'FAILED' = 'FAILED',
    'SKIPPED' = 'SKIPPED',
    'STOPPED' = 'STOPPED',
    'INTERRUPTED' = 'INTERRUPTED',
    'CANCELLED' = 'CANCELLED',
}